# Visual Skills

把讨论转为图：先确认要表达的关系和主张，再组织视觉。

本包包含 **3 个 skills、5 个按需加载配方和一组验收用例**。不是某种配色、模型或绘图工具的固定模板。

## 选择哪个 skill

| 需求 | Skill |
|---|---|
| 把系统、机制、差异、教学内容或证据讲清楚 | `visual-explain` |
| 给文章/观点配图，或用一组图推进叙事 | `visual-story` |
| 探索头像、封面、插画等视觉方向，或按参考重画 | `visual-art-direction` |

`visual-explain` 按需读取结构、流程、对比/演进、教学、证据五种配方。
“简化、重画、美化”属于所有技能共有的迭代行为，不另建重复 skill。

## 安装与使用
把三个命名目录复制到目标宿主支持的 skills 目录，保留目录内的 `SKILL.md` 和 `references/`。
具体发现/启用方式由宿主决定；此包不自动安装、连接账户或取得图像工具权限。
三个目录均包含独立的主指令。`visual-story` 可复用 `visual-explain`；缺少后者时，仍有最小正确性约束。
无需把评测目录放进宿主的自动加载目录；评测可留在仓库中单独运行。

示例请求：

```text
使用 visual-explain 把上面的方案画成结构图。
两个客户端都读写两个仓库，客户端之间没有直连。
```

```text
使用 visual-story 把这个观点做成四张连续的图，先生成第一张。
每张增加一个理解层次，不要四次总结同一段话。
```

```text
使用 visual-art-direction 基于当前参考图探索五种真正不同的插画风格。
保持主体与基本构图，只改变视觉语言。
```

## 工作方式

`问题与用途 → 语义/内容规格 → 视觉结构 → 实际渲染 → 验收 → 按反馈定向修改`

关键约束：不为排版补造关系；不因简化删除必要对象；不把制作指令画进正文；
不把概念示意伪装成实测；不把没有读取的图片当编辑目标；不因美化回退已确认的修正。

## 工具边界
技能描述决策与验收，不假定宿主一定有某个图像 API、文件写权限或发布能力。
解释性图像使用宿主支持的原生图像工具；精确数据、计数和几何由确定性方法保证。
工具路由、引用、图像生成后的回复与文件交付方式遵守宿主规则。
只要成图时不交付 Mermaid 代码或文字提示词充数；用户明确要求源文件时保留相应可编辑交付。

## 评测
见 [evals/README.md](../evals/visual-skills/README.md)、[验收用例](../evals/visual-skills/cases.json) 与 [规格走查](../evals/visual-skills/spec-replay.md)。
用例是合成或去标识化的行为测试，不含个人账号、私人仓库路径、家庭信息或聊天记录导出。
本包做过文件结构、前置字段、引用路径和用例格式检查；没有据此声称跨模型测试或图像回归已通过。

## 范围
本包关注作图行为，不附带生图服务、可编辑图形引擎或自动发布。
技术报告、文章写作和演示文件制作应由相应写作/制作技能负责；此包负责其中的视觉表达。
